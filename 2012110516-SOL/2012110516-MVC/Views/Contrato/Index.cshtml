@model IEnumerable<_2012110516_ENT.Entities.Contrato>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Venta.VentaId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaContrato)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Venta.VentaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaContrato)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ContratoId }) |
                @Html.ActionLink("Details", "Details", new { id=item.ContratoId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ContratoId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
