@model IEnumerable<_2012110516_ENT.Entities.Departamento>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.departamento)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.departamento)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.DepartamentoId }) |
                @Html.ActionLink("Details", "Details", new { id=item.DepartamentoId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.DepartamentoId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
